import 'package:flutter/cupertino.dart';

class FloatingButtonField extends StatelessWidget {
  const FloatingButtonField({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
